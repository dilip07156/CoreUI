@model string
@using (Html.BeginForm("", "", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, true, new { @class = "form-horizontal", role = "form", id = "frmProposalReview" }))
{
    <div class="wrapper">
        <div class="main-content form-theme-04 form">
            <div class="pg-container">
                <div class="sqr-tab-cont">
                    <div class="sqr-tab-data flights">
                        @*<div class="btn-cont-rgt" style="text-align:right">
                            <a href="#" class="btn btn-blue btn-md">Save</a>
                        </div>*@
                        <div class="process-data-sec">
                            <embed src="@Model" width="100%" height="1000" type='application/pdf'>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
